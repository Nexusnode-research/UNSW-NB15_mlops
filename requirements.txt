# Convenience shim — installs the full API + dev stack.
# Use specific files in requirements/ for targeted installs:
#   requirements/api.txt   — API serving only
#   requirements/ui.txt    — Dash UI only
#   requirements/train.txt — model training and export
#   requirements/dev.txt   — development tools (includes api.txt)
-r requirements/dev.txt
