# Deploy is manual only (Actions → this workflow → "Run workflow").
# Does not run on git push, to avoid accidental ECR/EKS rollouts.
name: UNSW-NB15_MLOPS — Build & Deploy to EKS

on:
  workflow_dispatch: {}

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_REGISTRY: ${{ secrets.ECR_REGISTRY }}
  EKS_CLUSTER: ${{ secrets.EKS_CLUSTER }}
  K8S_NAMESPACE: ${{ secrets.K8S_NAMESPACE }}
  API_IMAGE: unsw-nb15-mlops-api
  UI_IMAGE: unsw-nb15-mlops-ui
  IDS_API_TOKEN: ${{ secrets.IDS_API_TOKEN }}

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with LFS)
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Configure AWS credentials (secrets mode)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        run: |
          aws ecr get-login-password --region "$AWS_REGION" \
            | docker login --username AWS --password-stdin "$ECR_REGISTRY"

      # Build images (tag with SHA)
      - name: Build API image
        run: |
          docker build -f Dockerfile.api \
            -t "$ECR_REGISTRY/$API_IMAGE:latest" \
            -t "$ECR_REGISTRY/$API_IMAGE:${GITHUB_SHA}" .

      - name: Build UI image
        run: |
          docker build -f Dockerfile.ui \
            -t "$ECR_REGISTRY/$UI_IMAGE:latest" \
            -t "$ECR_REGISTRY/$UI_IMAGE:${GITHUB_SHA}" .

      # --- Local smoke test for API before pushing ---
      - name: Start API under test (local)
        run: |
          docker run -d --rm --name api-under-test \
            -e IDS_API_TOKEN="${IDS_API_TOKEN}" \
            -p 8000:8000 \
            "$ECR_REGISTRY/$API_IMAGE:${GITHUB_SHA}"

      - name: Wait for /health (public endpoint, no auth required)
        run: |
          for i in {1..90}; do
            if curl -fsS http://localhost:8000/health >/dev/null; then
              echo "API is healthy"; exit 0
            fi
            if ! docker ps --format '{{.Names}}' | grep -q '^api-under-test$'; then
              echo "Container exited, logs follow:"; docker logs api-under-test || true; exit 1
            fi
            sleep 2
          done
          echo "Timed out waiting for API"; docker logs api-under-test || true; exit 1

      - name: Install Newman
        run: npm install -g newman

      - name: Generate env.json for Newman
        run: |
          mkdir -p test-results
          cat > env.json <<'EOF'
          {
            "id": "ci-env",
            "name": "CI",
            "values": [
              {"key":"baseUrl","value":"http://localhost:8000","type":"text","enabled":true},
              {"key":"apiToken","value":"__TOKEN__","type":"text","enabled":true}
            ]
          }
          EOF
          sed -i "s/__TOKEN__/${IDS_API_TOKEN}/" env.json

      - name: Run Newman smoke tests
        run: |
          newman run ids_unsw/tests/UNSW_IDS_API.postman_collection.json \
            -e env.json \
            --reporters cli,junit \
            --reporter-junit-export test-results/newman.xml

      - name: Upload Newman report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: newman-report
          path: test-results/newman.xml

      - name: Stop API under test
        if: always()
        run: docker stop api-under-test || true

      # --- Push images after tests pass ---
      - name: Push API image
        run: |
          docker push "$ECR_REGISTRY/$API_IMAGE:latest"
          docker push "$ECR_REGISTRY/$API_IMAGE:${GITHUB_SHA}"

      - name: Push UI image
        run: |
          docker push "$ECR_REGISTRY/$UI_IMAGE:latest"
          docker push "$ECR_REGISTRY/$UI_IMAGE:${GITHUB_SHA}"

      # --- Deploy to EKS ---
      - name: Install kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: "latest"

      - name: Update kubeconfig
        run: aws eks update-kubeconfig --region "$AWS_REGION" --name "$EKS_CLUSTER"

      - name: Apply Kustomize overlay
        run: kubectl apply -k k8s/overlays/eks-dev

      - name: Roll deployments to new images by commit
        run: |
          kubectl -n "$K8S_NAMESPACE" set image deploy/ids-api  api="$ECR_REGISTRY/$API_IMAGE:${GITHUB_SHA}"
          kubectl -n "$K8S_NAMESPACE" set image deploy/dash-ui ui="$ECR_REGISTRY/$UI_IMAGE:${GITHUB_SHA}"

      - name: Wait for rollouts
        run: |
          kubectl -n "$K8S_NAMESPACE" rollout status deploy/ids-api
          kubectl -n "$K8S_NAMESPACE" rollout status deploy/dash-ui
